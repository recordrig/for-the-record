export function getStaticProps() {
  return {
    props: {
      datePublished: "20200730",
      layout: "Blog",
      lang: "EN",
      title: "A post is a post"
    },
  };
}

# A post is a post

And that's that.
