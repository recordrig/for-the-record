export function getStaticProps() {
  return {
    props: {
      datePublished: "20200730",
      layout: "Blog",
      lang: "NL",
      title: "Een post is een post"
    },
  };
}

# Een post is een post

En dat is dat.
